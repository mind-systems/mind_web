# Mind Web — Roadmap

## Phase 1 — Project Bootstrap

- [x] **Scaffold Vite + React + TypeScript project** — Bootstrap the empty repo with Vite + React + TypeScript; install runtime deps (`react-router-dom`, `@tanstack/react-query`, `echarts`, `echarts-for-react`); strip generated boilerplate. Full spec and constraints in `notes/02-scaffold-vite-react-typescript-project.md`. Task ends when `npm run dev` starts a blank page with no console errors. [28m 12s]

- [x] **Configure TailwindCSS** — Install `tailwindcss@3 postcss autoprefixer` as dev deps (pin to v3 — bare `tailwindcss` resolves v4 which has incompatible setup). Run `npx tailwindcss init -p`. Set `content: ["./index.html", "./src/**/*.{ts,tsx}"]` in `tailwind.config.js`. Add three separate directives to the top of `src/index.css`: `@tailwind base;`, `@tailwind components;`, `@tailwind utilities;`. Replace `index.html` title with `Mind`. Full setup spec and v3 vs v4 gotchas: `notes/03-tailwindcss-v3-setup.md`. Task ends when a `className="text-blue-500"` element renders in the correct color. [9m 56s]

- [x] **Configure project structure and environment** — Create directories: `src/core/api/`, `src/core/auth/`, `src/core/types/`, `src/pages/`, `src/components/`. Create `.env.local` (gitignored) with `VITE_API_BASE_URL=http://localhost:3002`. Create `.env.example` with the same key and an empty value. Create `src/core/config.ts` exporting `const API_BASE_URL = import.meta.env.VITE_API_BASE_URL as string` with a startup assertion that it is non-empty. Configure `@` path alias: add `resolve.alias` to `vite.config.ts` (requires `npm install -D @types/node` for `import { resolve } from 'path'`); add `paths` and `baseUrl` to `tsconfig.app.json` (not the root `tsconfig.json` — the modern Vite template's app code compiles under `tsconfig.app.json`). Task ends when `import { API_BASE_URL } from '@/core/config'` resolves without errors. [6m 57s]

- [x] **Configure React Router and page shells** — Create `src/router.tsx` with `createBrowserRouter`. Routes: `/login` → `LoginPage` (public), `/deeplink-auth` → `MagicLinkPage` (public), `/auth/google/callback` → `GoogleCallbackPage` (public), `/sessions` → `SessionsPage` (protected), `/sessions/:id` → `SessionsPage` with selected session (same component, protected), `/calibrations` → `CalibrationPage` (protected), `/` → redirect to `/sessions`. `ProtectedRoute` redirects to `/login` when token absent. Create stub components for each page. Wrap `src/main.tsx` with `RouterProvider`, `QueryClientProvider`, `AuthProvider`. Task ends when navigating to each URL renders the correct stub. [7m 34s]

## Phase 2 — Authentication

- [x] **Auth context + token management** — Create `src/core/auth/AuthContext.tsx`. Context value: `{ token: string | null; login(token: string): void; logout(): void; pendingEmail: string | null; setPendingEmail(email: string): void; clearPendingEmail(): void }`. `token` persisted under `mind_auth_token`; `pendingEmail` under `mind_pending_email` — both in `localStorage`, both managed exclusively by `AuthContext` (no other file may read or write these keys). `login()` stores token and navigates to `/sessions`. `logout()` clears token and navigates to `/login`. Export `useAuth()` hook. `ProtectedRoute` reads from `useAuth().token`. Task ends when `useAuth().logout()` from a protected page redirects to `/login`. [13m 24s]

- [x] **Login page: email OTP flow** — Implement `LoginPage`. Step 1: email input + "Send code" button → `POST /auth/send-code { email }`. On success: call `auth.setPendingEmail(email)` (persists under `mind_pending_email` via AuthContext — do not write to localStorage directly), advance to step 2. Step 2: 6-digit code input + "Verify" button → `POST /auth/verify-code { email, code }` returns `{ accessToken, user }` → `auth.login(accessToken)`. Show loading spinner during requests. Show inline error on failure (extract `message` field from response body). Centered card, max-width 400px. Note: these REST endpoints live in mind_api Phase 21. [9m 12s]

- [x] **Magic link auto-login** — Implement `MagicLinkPage` at `/deeplink-auth`. The API email template already contains `{APP_BASE_URL}/deeplink-auth?code=XXXXXX` — on mobile this opens as a native deeplink, in a browser this hits this route. On mount: read `?code=` from URL query params. Read `auth.pendingEmail` (set by `LoginPage` via `auth.setPendingEmail()`). If both present: call `POST /auth/verify-code { email, code }` → `auth.login(token)` → navigate to `/sessions`. If code is present but `auth.pendingEmail` is null: show a short email input so the user can confirm their address, then verify. Call `auth.clearPendingEmail()` after successful login. No changes to API email templates needed — the existing deeplink URL works for web as-is. [5m 58s]

- [x] **Login page: Google Sign-In** — Add "Continue with Google" button on `LoginPage`. On click: call `GET /auth/google` → API redirects browser to Google OAuth. After Google auth, API relays to `{WEB_BASE_URL}/auth/google/callback?googleCode=<code>` (success) or `?googleError=<err>` (failure). Implement `GoogleCallbackPage`: on `googleCode` → call `POST /auth/google { code, redirectUri: window.location.origin + '/auth/google/callback' }` → receive `{ accessToken, user }` → `auth.login(accessToken)`. On `googleError` → redirect to `/login?error=google`. Note: endpoint spec in mind_api Phase 21 (`.ai-factory/ROADMAP.md`). [14m 24s]

- [x] **API client with auth interceptor** — Create `src/core/api/client.ts`. `apiFetch<T>(path, options?)`: prepend `API_BASE_URL`, add `Content-Type: application/json` and `Authorization: Bearer <token>` headers. On 401: clear token, navigate to `/login`. On non-2xx: throw `ApiError(status, message)` with message extracted from response JSON. Task ends with the client used by at least one page. [6m 54s]

## Phase 3 — Sessions Split-Panel Page

Single page at `/sessions` (and `/sessions/:id` for selected state). Left column: session list. Right panel: session charts. Selecting a session updates the URL to `/sessions/:id` and populates the right panel — no page navigation.

- [x] **Split-panel layout shell** — Implement `SessionsPage` with a two-column layout: left column fixed-width (~280px), right panel fills remaining width. Left column header: "Sessions" title + logout button. Right panel: empty state ("Select a session") when no session selected. Use TailwindCSS `flex h-screen overflow-hidden`. Left column is scrollable independently. Right panel is scrollable independently. [3m 30s]

- [x] **Session list (left column)** — Fetch `GET /sessions/runs` via React Query. Render a scrollable list of session rows: date (`DD MMM, HH:mm`) + duration (`mm:ss`). Selected session highlighted. Clicking a row navigates to `/sessions/:id` (via React Router, no reload). "Load more" at the bottom loads next page. Skeleton loader while fetching. "No sessions yet" empty state. [6m 54s]

- [x] **Session charts panel (right panel) — ECharts multi-grid** — When `/sessions/:id` is active, right panel fetches `GET /sessions/runs/:id/instructions` and `GET /sessions/runs/:id/biometrics` in parallel (full session range: `?from=startedAt&to=endedAt`; no incremental loading for MVP). Renders a single ECharts instance with up to 4 stacked grids sharing a linked X-axis (seconds from session start): (1) instruction phases — colored bars for `inhale`/`hold`/`exhale`/`rest` from `breath_phase` instructions; (2) heart rate from `sampleType: "cardio"` → `data.heartRate`; (3) EEG bands from `sampleType: "nfb"` → `data.{delta,theta,alpha,smr,beta}`; (4) emotions from `sampleType: "emotions"` → `data.{attention,relaxation,cognitiveLoad,cognitiveControl,selfControl}`. Omit any grid whose sample type has zero data points. All X-axes linked via `dataZoom` and `axisPointer.link`. Panel header: formatted date + total duration. Full spec: color palette, ECharts option skeleton, data transforms, dynamic grid layout in `notes/04-echarts-session-charts.md`. [18m 31s]

## Phase 4 — NFB Calibration History Page

- [x] **`/calibrations` page** — Fetch `GET /nfb-calibrations` (returns `{ records, total }`); load more with `?offset=N` as the user scrolls down. Group records by `deviceSerial`. For each device: section header with serial + "valid / total" badge. One ECharts `LineChart` (X-axis: `calibratedAt` ordered chronologically, Y-axis: value) with two lines: `individualFrequency` (Hz, alpha frequency) and `individualPeakFrequencyPower` (alpha peak power) — these values shift with each new user connection and the chart shows their evolution over calibration history. Invalid calibrations: hollow red dots. Valid: filled green dots. Tooltip shows `calibratedAt`, `isValid`, `failReason`. Empty state: "No calibrations recorded yet". [12m 3s]

## Phase 5 — Bug Fixes & Cleanup

- [x] **Fix Google OAuth callback parameter names** — `GoogleCallbackPage` reads `?code=` and `?error=` but the API relays `?googleCode=` and `?googleError=`; Google sign-in is completely broken. Two-line fix in `src/pages/GoogleCallbackPage/index.tsx`: `searchParams.get('code')` → `'googleCode'`, `searchParams.get('error')` → `'googleError'`. Do not change `LoginPage`'s `?error=google` round-trip or `MagicLinkPage`'s `?code=` — those are correct. Full spec: `notes/05-google-callback-fix.md`. [4m 13s]

- [x] **Fix auth state cleanup on logout and 401** — Two bugs: (1) `logout()` in `AuthContext` does not clear `mind_pending_email`, leaving a stale email that silently mismatch-verifies on the next magic-link visit; (2) `client.ts` throws `ApiError` after `window.location.assign('/login')`, racing callers' catch handlers with the hard navigation. Fix (1): inline `localStorage.removeItem(PENDING_EMAIL_KEY)` and `setPendingEmailState(null)` in `logout`. Fix (2): return `new Promise<T>(() => {})` after the `window.location.assign` call so no catch handler fires. Full spec: `notes/06-auth-state-cleanup.md`. [4m 41s]

- [x] **Optimize chart transforms and simplify chartOption** — Four independent improvements surfaced by code review: (1) Pre-partition the biometrics array by `sampleType` once (one `Map` pass) and hoist `startMs = new Date(startedAt).getTime()` before calling `toSeries` — eliminates 11× full-array scans and O(n×11) `Date` constructions; update `toSeries` signature to accept pre-filtered samples and `startMs: number`. (2) Wrap the `buildSessionChartOption` call in `SessionCharts.tsx` with `useMemo`. (3) Remove `orderMap` and the dead `.sort()` from `groupByDevice` in `CalibrationPage/transforms.ts` — `Map` iteration is already insertion-ordered. (4) Replace `allXAxisIndices` array in `chartOption.ts` with the ECharts built-in string `'all'`. Full spec: `notes/07-chart-performance-and-simplification.md`. [7m 14s]

- [x] **Shared format utility and query co-location** — Three cleanup items: (1) Create `src/core/format.ts` with `formatDate` and `formatDuration`; delete the duplicate `format.ts` files in `SessionsPage/` and `CalibrationPage/`; update 4 import sites. (2) Extract a shared `<PageHeader title={...} />` component in `src/components/` from the identical header block copied in both `SessionsPage` and `CalibrationPage`. (3) Move the instructions and biometrics `useQuery` calls from `SessionsPage/index.tsx` into `SessionCharts.tsx` — they are the chart panel's data concern; the current location couples chart-data fetches to `SessionList` re-renders. `SessionChartsProps` shrinks to `{ session: SessionRun }`. Full spec: `notes/08-shared-format-and-query-location.md`. [13m 6s]

## Phase 6 — Module Awareness & Navigation

- [x] **Extend SessionRun type and enrich the session cell** — `GET /sessions/runs` items include `activityType: 'breath' | 'meditation'`, `description: string | null`, and `complexity: number | null`. Add these to the `SessionRun` interface in `src/core/types/index.ts` (plus an exported `ActivityType` alias). Add a `ModuleBadge` component in `src/components/`. Redesign the bare `SessionList.tsx` row (currently `formatDate` + `formatDuration` only) to show: line 1 — session title (breath → `description`; meditation → `'Meditation'`, truncated) + module badge; line 2 — date · duration · `Difficulty {complexity}` (breath only). Full spec: `notes/09-module-aware-session-list.md`. [9m 18s]

- [x] **Module filter tabs in the session list** — The left-column list mixes breath and meditation with no way to focus on one. Add an `All / Breath / Meditation` segmented control above `<SessionList>` in `SessionsPage/index.tsx`, filtering the loaded `sessions` array by `activityType` via local `useState` (no query change). Preserve reverse-chronological order and the existing `getNextPageParam`/"Load more" logic. When the filtered view is empty but sessions exist, show a "No {module} sessions" message. Full spec: `notes/09-module-aware-session-list.md`. [7m 43s]

- [x] **Module-aware session detail panel** — `SessionCharts.tsx` header shows only date + duration, and `chartOption.ts` always allocates the breath-phase instruction grid (index 0) — meditation sessions have no `breath_phase` instructions and get an empty 80px band. (1) Header: add `ModuleBadge`, title (`description ?? 'Meditation'`), and `Difficulty` for breath. (2) `chartOption.ts`: add a `hasPhases = phases.length > 0` flag and make the instruction grid conditional, threading it through grid-index assignment, `gridHeights`, x/y axes, and the phase series — exactly like the existing `hasHeartRate`/`hasEeg`/`hasEmotions` flags. Meditation then starts directly with biometric grids. Full spec: `notes/10-module-aware-session-detail.md`. [7m 28s]

- [x] **Top navigation between Sessions and Calibrations** — `/calibrations` is unreachable from the UI; no link points to it. Turn `PageHeader` into a top bar: drop the `title` prop, render two `NavLink`s ("Sessions" → `/sessions`, "Calibrations" → `/calibrations`) with active-state styling, keep Log out on the right. Update both pages to render `<PageHeader />` without a title; in `SessionsPage` move the header to span full width above the split panel. Full spec: `notes/11-top-navigation.md`. [4m 5s]

## Phase 7 — Module UI Review Follow-Up

- [x] **Fix module label and empty-state bugs in session detail** — Two correctness bugs from the Phase 6 review. (1) `SessionCharts.tsx` falls back to the literal `'Meditation'` for a null `description`, so a breath session with a soft-deleted `breath_sessions` row shows title "Meditation" next to a "Breath" badge; extract `sessionTitle(session)` into `src/pages/SessionsPage/sessionTitle.ts` and use it in both `SessionCharts.tsx` and `SessionList.tsx`. (2) `isEmpty` in `SessionCharts.tsx` keys off raw `instructionsData.length`, but the server writes `session_event` instructions for every session — a meditation session recorded without a BCI (instructions present, no `breath_phase`, no biometrics) renders a blank ~90px chart instead of "No data"; have `buildSessionChartOption` return `gridCount: totalGrids` and compute `isEmpty = !isLoading && !isError && gridCount === 0`. Full spec: `notes/12-module-ui-review-followup.md`. [4m 59s]

- [x] **Harden module filter empty-state and badge fallback** — Two robustness gaps from the Phase 6 review. (1) The filter empty-state in `SessionsPage/index.tsx` asserts "No {module} sessions" even when `hasNextPage` is true and matching sessions may live in unloaded pages (with a "Load more" button rendered right below the message); make the message conditional on `hasNextPage` ("…loaded yet — load more below" vs "No {module} sessions"). (2) `ModuleBadge.tsx` indexes `STYLES[type]`/`LABELS[type]` with no fallback, so an unknown `activityType` (the API enum is the documented extension point for future types) renders `class="… undefined"` and an empty label; add `LABELS[type] ?? type` and `STYLES[type] ?? 'bg-gray-100 text-gray-600'`. Full spec: `notes/12-module-ui-review-followup.md`. [3m 54s]

- [x] **Minor session-list polish from review** — Two low-severity items from the Phase 6 review. (1) Cosmetic: the `SessionList.tsx` empty branch dropped `h-full` in Phase 6, so the empty message no longer centers vertically — restore `flex h-full flex-col items-center justify-center`. (2) `complexity === 0` renders "Difficulty 0.0" in both `SessionList.tsx` and `SessionCharts.tsx`; first confirm with the API/product side whether `0` is a real difficulty or a "not set" sentinel — if a sentinel, tighten the guard from `complexity != null` to `complexity != null && complexity > 0` in both files (do not change blindly). Full spec: `notes/12-module-ui-review-followup.md`. [4m 20s]

## Phase 8 — Security

- [x] **OAuth `state` CSRF protection (SPA side)** — The Google sign-in relay carries no `state`, leaving it open to login-CSRF: an attacker's `code`, fed to the victim's callback, silently logs the victim into the attacker's account. Today `LoginPage.handleGoogleSignIn` does `window.location.href = ${API_BASE_URL}/auth/google` with no state, and `GoogleCallbackPage` exchanges `googleCode` with no state check. The SPA is the OAuth client and owns `state` (backend holds no session). (1) Add `src/core/auth/oauthState.ts` with `createOAuthState()` (generate a `crypto.randomUUID()` opaque token, store under `sessionStorage` key `oauth_state`, return it) and `consumeOAuthState()` (read-and-remove, one-time use); use `sessionStorage` (tab-scoped), not `localStorage`, and keep this helper the only place that touches it so no page reads storage directly. Note on the project rule: `CLAUDE.md`'s storage rule is a closed allow-list — `localStorage` access only in `core/auth/AuthContext.tsx` and `core/api/client.ts` — and is specifically about `localStorage`, so a new `sessionStorage` helper does not break it literally; to honor its spirit (storage centralized in dedicated modules), at implementation time either add `oauthState.ts` to that allow-list in `CLAUDE.md` (clarifying it also covers `sessionStorage`) or put the two helpers directly in `AuthContext.tsx`. (2) `LoginPage.handleGoogleSignIn`: `const state = createOAuthState();` then redirect to `${API_BASE_URL}/auth/google?state=${encodeURIComponent(state)}`. (3) `GoogleCallbackPage`: after the existing `didExchange` ref guard (so StrictMode's double-invoke does not consume the one-time state twice), read `state` from the query and compare to `consumeOAuthState()` **before** anything else — if missing or mismatched, `navigate('/login?error=google', { replace: true })` and do NOT call `POST /auth/google`; if it matches, proceed with the existing `googleCode`/`googleError` handling and code exchange. Optionally include `state` in the `POST /auth/google` body (backend logs it, does not validate). Ships in lockstep with mind_api Phase 26 (backend relays `state` through `GET /auth/google` and the callback, and accepts it unvalidated on `POST /auth/google`) — neither half does anything until both ship. Full spec: `notes/13-oauth-state-csrf-requirements.md`. [5m 16s]

## Phase 9 — Dependency Health

- [x] **Replace echarts-for-react with custom EChart wrapper** — `echarts-for-react@3.0.6` is abandoned (last real release 2021); its compiled output imports `from "tslib"` without declaring it as a dependency, which Vite 8's Rolldown bundler cannot resolve. Create `src/components/EChart/index.tsx`: props `option: EChartsOption`, `style?: CSSProperties`, `notMerge?: boolean`; `useEffect` on mount calls `echarts.init(divRef.current)`, second effect keyed on `option` calls `chart.setOption(option, notMerge ?? false)`, cleanup calls `chart.dispose()`; `ResizeObserver` calls `chart.resize()`. Remove `echarts-for-react` from `package.json`. Swap the import and JSX tag in `SessionCharts.tsx` (line 3 + 95) and `CalibrationChart.tsx` (line 1 + 22–24). No chart behavior changes. Spec: `.ai-factory/notes/14-replace-echarts-for-react.md`. [9m 44s]

## Phase 10 — UI Polish

- [x] **Auto dark/light theme** — No dark mode today; all components use hardcoded light-mode Tailwind classes and `EChart` calls `echarts.init()` with no theme. (1) Add `darkMode: 'media'` to `tailwind.config.js` — OS preference drives all `dark:` variants, no JS toggle needed. (2) Update `src/components/EChart/index.tsx`: detect `window.matchMedia('(prefers-color-scheme: dark)').matches` and pass `'dark'` to `echarts.init()`; subscribe to the `change` event to dispose and re-init the chart when OS theme switches at runtime. (3) Add `dark:` variants to 13 UI files following the consistent color mapping in the spec (bg-white→dark:bg-gray-900, text-gray-900→dark:text-gray-100, border-gray-200→dark:border-gray-700, inputs get dark:bg-gray-800, ModuleBadge colored variants shift to 800-level, SkeletonLoader pulse shifts to dark:bg-gray-700). Spec: `.ai-factory/notes/15-auto-dark-light-theme.md`. [18m 18s]

## Phase 11 — Instruction Stream Display

- [x] **Fix InstructionDto field names** — `InstructionDto` in `src/core/types/index.ts` declares `type` and `payload`, but the API stores and returns `instructionType` and `data`. `parsePhases` in `transforms.ts` filters by `i.type === 'breath_phase'` — always empty — so the instruction grid never renders for any breath session. Rename `type` → `instructionType`, `payload` → `data: { phase?, durationMs? }` in the interface; update `parsePhases` to match. No API change needed. Spec: `.ai-factory/notes/18-fix-instruction-dto-fields.md`. [5m 19s]

- [x] **Phase text labels in instruction grid** — Colored bars show which phase is active but carry no readable label; the user cannot tell inhale from exhale without a legend. Change `renderItem` in `chartOption.ts` to return a `group` with a `rect` (existing bar) plus a `text` child ("Inhale", "Hold", "Exhale", "Rest") positioned at the left edge of the bar, white bold 11 px. Suppress text when bar width < 40 px. Extend `PhaseBar` with `durationMs?: number` and populate it from `event.data.durationMs` in `parsePhases`. Add `PHASE_LABELS` map alongside `PHASE_COLORS`. Depends on the InstructionDto fix above. Spec: `.ai-factory/notes/19-phase-text-labels.md`. [10m 54s]

## Phase 12 — Biometric Data Loading

- [x] **Add motion sensor grid to session chart** — `chartOption.ts` ignores `sampleType: 'motion'` today; the mobile app sends ax/ay/az (accelerometer) and gx/gy/gz (gyroscope) for every breath session with a connected device. Add `const motion = byType.get('motion') ?? []`, extract six `toSeries` calls, add `hasMotion` flag, and wire up a new MOTION_GRID (index inserted after EMOT_GRID) with its own Y-axis (`scale: true`, name `'m/s²·rad/s'`) and six line series using the color palette in the spec. Thread the new grid through `gridHeights`, `gridTops`, `grids`, `xAxes`, `yAxes`, `allSeries`, and `totalGrids` — same conditional pattern as HR_GRID/EEG_GRID/EMOT_GRID. Spec: `.ai-factory/notes/16-motion-grid.md`. [5m 35s]

- [x] **Chunked biometric loading with zoom-driven trigger** — The single full-session biometrics fetch causes 413 for real sessions (motion alone ≈ 250 samples/s). Replace the biometrics `useQuery` in `SessionCharts.tsx` with a new `useBiometricChunks(session)` hook (`src/pages/SessionsPage/useBiometricChunks.ts`) that loads 30 s chunks sequentially (one in-flight, CHUNK_SEC = 30, ref-based dedup, half-open windows, per-fetch ignore guard, skip chunks where `fromMs >= sessionEndMs`). Chunk 0 on mount; on ECharts `datazoom` enqueue overlapping unloaded indices. Add an `onEvents` prop to `src/components/EChart/index.tsx` bound in a **separate** effect (`off`-before-`on`), never in the init/dispose effect. Sort pairs in `toSeries` (chunks arrive out of order). Thread a `zoom` param into both `dataZoom` entries in `chartOption.ts` and render the chart with **`notMerge` true (full rebuild)** — NOT `notMerge:false`/`replaceMerge`: both were measured to cross-wire series→axis links when a sampleType first appears in a later chunk. Per-session `key` on `<SessionCharts>`; chunk errors soft; don't show "No data" until all chunks attempted. API endpoint unchanged. Spec: `.ai-factory/notes/17-chunked-bio-loading.md`. [23m 38s]

## Phase 13 — Observability logging: OTLP sink swap

Integrate the `observe-js` (v0.1.0, browser layer) OTLP/HTTP SDK as a **pure transport swap** for existing logs — ship them to the shared local Loki backend tagged `project=mind`, `service_name=mind_web`. First **browser** integration in the family — sets the pattern tradeoxy_gui copies. Pure transport + a thin facade; no new log lines, no call-site coupling, no `withSpan` in business code. Source: `~/projects/observability/.ai-factory/notes/03-integrate-mind-web.md` (DoD #1/#2/#4). Closes the mind_web box on the observability ROADMAP (line 36). Confirmed decisions: env names `VITE_LOG_DESTINATION`/`VITE_OTLP_ENDPOINT` (Vite prefix mandatory), default destination hard `file`, CORS via Vite dev proxy (relative endpoint, zero backend change), facade named `logger`. Atomicity Gate: lifecycle (task 1) ships and is observable via `service.start` without the facade; the facade + repoint (task 2) ships on top — two atomic tasks.

- [x] **Stand up the SDK lifecycle — dependency, `VITE_LOG_DESTINATION`/`VITE_OTLP_ENDPOINT` resolver, idempotent `init`, Vite dev proxy, env docs** — No `observe-js` dep, no logging init, no OTLP endpoint exist. Add `observe-js` as a git dep pinned to `v0.1.0` (`git+https://github.com/mind-systems/observe-js.git#v0.1.0`; Vite resolves the `browser` export). Create `src/core/observe/`: `config.ts` resolving `VITE_LOG_DESTINATION` (∈ `file|grafana|both`, default **`file`**) → `logToConsole`/`logToObserver` and `VITE_OTLP_ENDPOINT` → `otlpEndpoint` — **never throws** (unlike `config.ts`); `init.ts` exporting `initObserve()` that calls SDK `init({project:'mind',service:'mind_web',endpoint})` gated on `logToObserver && otlpEndpoint`, idempotent. Call `initObserve()` at the top of `src/main.tsx` before `createRoot`. Add a `server.proxy` `/otlp` → `http://localhost:3100` to `vite.config.ts` (same-origin relative endpoint → no preflight, `sendBeacon` works). Update `.env.example`; add an env-contract doc. Guards: ZERO app log lines (facade is task 2); resolver never throws; do NOT implement unload/flush (browser `init` registers it); default hard `file`. Spec: `.ai-factory/notes/20-observe-sink-lifecycle-init.md`. [14m 40s]

- [x] **Introduce the `logger` facade and repoint the existing `console.error`** — mind_web has no project logger and one real `console.*` site (`src/pages/SessionsPage/useBiometricChunks.ts:118`). Create `src/core/observe/logger.ts` exporting `logger.info/warn/error(msg, attrs?)` that routes to `console.*` and/or SDK `log(level, msg, attrs)` per the note-20 resolver (reuse `logToConsole`/`logToObserver`, do not re-read env); re-export from `src/core/observe/index.ts`. Repoint the chunk-drain `console.error` to `logger.error('Failed to load biometric chunk …', { err })`, keeping it inside the existing `.catch` after the `fetchIdRef` stale-guard (behavior unchanged). Guards: ZERO new log lines (only reroute the one site); facade name `logger` (no SDK `log` shadow); no `try/catch` around `log`. Depends on note 20. Spec: `.ai-factory/notes/21-observe-logger-facade.md`. [14m 43s]

## Phase 14 — Observability logging: outbound trace origination

Originate a per-request `trace_id` in the HTTP layer so mind_api can stamp its logs with the originating request's trace — **zero new log lines, zero call-site changes**. Browser counterpart to mind_mobile note 112 (gRPC inject); receiving leg is mind_api Phase 35. **Droppable**: the browser ambient context (explicit, non-`zone.js`) does not survive `await`, so correlation is **one-way** (mind_api inherits; the browser-side error log does not) — that is the honest floor. Never add a log line to force two-way. Gated on Phase 13.

- [x] **Mint a span + inject `traceparent` inside `apiFetch`** — Outgoing calls carry no trace context today. `apiFetch` in `src/core/api/client.ts` is the single HTTP choke point (raw `fetch` is banned in pages by `CLAUDE.md`), so trace origination lives **here — infrastructure, configured once**, never in `onClick`/components/business code (the rejected mobile-agent mistake). Build the merged headers (`Content-Type`/`Authorization`/caller headers) first, then — gated on `logToObserver` — wrap header construction in `withSpan(startSpan(), () => withTraceparent(baseHeaders))` and pass the result to the existing `fetch`. All synchronous **before** the `await`, so the `trace_id` is captured into the request headers within the sync/microtask window; mint per call (no reuse, no app-wide `runWithContext`). Imports from `observe-js` + `logToObserver` from `@/core/observe/config`. Guards: ZERO new logs; no `withSpan` around action handlers; inject stays synchronous before the await; one-way is the floor — if undesirable, drop Phase 14 and keep the Phase 13 sink. Spec: `.ai-factory/notes/22-apifetch-trace-origination.md`. [16m 40s]

## Phase 15 — Client offset timeline for breath phases

Web counterpart to the unified session-time model (mind_mobile Phase 42 / mind_api note 49). Phase per-sample geometry moves onto the client-owned monotonic `offsetMs` axis (mind_mobile note 121), killing the `clientWallClock − serverStartedAt` cross-clock subtraction that lagged breath start by ~1.5 s. `startedAt` survives on the web in exactly **one** place — the axis-length scalar `endedAt − startedAt` that bounds the last bar — never in per-sample geometry. Biometrics are deliberately **not** re-anchored: after the mobile emit-at-origin fix the residual phase↔bio offset is sub-second (phone↔BCI skew + RTT), accepted as invisible. Sequence after mind_mobile Phase 42 ships `data.offsetMs`.

- [x] **Move breath-phase geometry to the `offsetMs` axis** — `parsePhases` (`src/pages/SessionsPage/transforms.ts:13-34`) computes per-sample boundaries via `secFromStart(event.timestamp, startedAt)` (cross-clock). Switch `startSec`/`endSec` to `(event.data.offsetMs ?? 0) / 1000`; keep `secFromStart(endedAt, startedAt)` **only** for the last-bar end (the axis-length scalar — no terminal marker, no end-cap, no clamp, abandoned/disconnected run to the inflated end). Drop `data.durationMs` + `PhaseBar.durationMs` (gone in mind_mobile note 121); recompute the in-bar `· Ns` label in `chartOption.ts:316-321` from `endSec − startSec`. Add `offsetMs?`/`tickCount?` to `InstructionDto.data` in `src/core/types/index.ts`. `toSeries`/bio untouched. Hard cutover — no legacy `timestamp` fallback (pre-migration data is stale). Spec: `.ai-factory/notes/23-phase-geometry-on-offset-axis.md`. [5m 29s]

- [x] **Render `phase='pause'` as a styled band** — mind_mobile note 124 emits pause/resume as `breath_phase` markers with `data.phase='pause'`; today they fall through to `#ccc` / raw `"pause"` label (`chartOption.ts:317,338`). Add `pause` to `PHASE_COLORS`/`PHASE_LABELS` (`chartOption.ts:5-17`) with a distinct neutral tone (not `rest`'s `#9E9E9E`) + `'Pause'` label; widen the phase type to admit `'pause'` (prefer `PhaseKind = BreathPhase | 'pause'` in `src/core/types/index.ts`, keeping `BreathPhase` breath-only). `parsePhases` passes `data.phase` through verbatim — no parsing change. Keep the `?? '#ccc'`/`?? bar.phase` fallbacks. Optional: suppress the duration suffix for pause bars. Depends on note 23 + mind_mobile note 124. Spec: `.ai-factory/notes/24-pause-phase-band.md`. [4m 43s]

- [x] **Fix first `rest` phase vanishing from the timeline** — two offset-axis fallout bugs. (1) The phase custom series sits under a shared `dataZoom` with the default `filterMode:'filter'`, which deletes any item whose anchor leaves the window — the first `rest` at `offsetMs≈0` sits on the axis min and got culled; set `filterMode:'none'` on both dataZoom entries + `clip:true` on the phase series (`chartOption.ts`) so bars clip to the grid instead of vanishing/overflowing. (2) `SessionCharts.tsx` fetched instructions with `from=startedAt`, but a phase's wire `timestamp` can precede `startedAt` (first `rest` by ~0.5 s), so the API dropped it — fetch instructions with no `from/to` window (small payload, no 413 risk). Verified live on session `8e2978a1`.

- [x] **Load the visible window's biometric chunks on mount** — the chunked loader fetched only chunk 0 on mount and deferred the rest to `datazoom` events, so a session showed just its first 30 s of biometrics until the user scrolled. Extract the window→chunks request into `requestWindowChunks` and call it on mount with the initial zoom window (`SessionCharts.tsx`); at the default 0–100 % zoom this enqueues every overlapping chunk, filling the whole visible range without interaction. Chunks still drain one at a time (per-chunk 413 bound intact), and `handleDataZoom` reuses the same helper. Removes the old `gridCount === 0` eager-drain (now subsumed). Verified live on session `8e2978a1`.

- [x] **Refine session chart presentation** — three readability fixes in `chartOption.ts`. (1) Drop the Y-axis `splitLine` horizontal gridlines on the data grids (HR/EEG/emotions/motion). (2) Move each Y-axis unit name (`BPM`/`μV`/`Score`/`m/s²·rad/s`) off the scale — `nameLocation:'middle'` + `nameRotate:90` + `nameGap:40` so it sits vertically left of the tick labels instead of colliding with them and the grid above. (3) Per-grid tooltip: drop `axisPointer.link:[{xAxisIndex:'all'}]` (both the tooltip's and the global one) so hovering a grid shows only that grid's series rather than every series stacked across all grids; zoom stays synced via the independent `dataZoom (xAxisIndex:'all')`, only the crosshair stops mirroring onto other grids. Verified live on session `8e2978a1`.

## Phase 16 — Feature: Delete a session from the list

The dashboard can view sessions but not remove them, so test/unwanted runs pile up. Add a hover-revealed delete affordance on each list row with a confirmation step that permanently removes the run and all its captured data via the backend. Depends on mind_api Phase 45 (`DELETE /sessions/runs/:id`). Context: `.ai-factory/notes/25-delete-session-from-list.md`.

- [x] **Hover delete button + confirm dialog + delete mutation** — `SessionList.tsx` rows are whole-card `<Link to={/sessions/:id}>` with no delete control; `SessionsPage/index.tsx` loads the list via `useInfiniteQuery(['session-runs'])`; the app has no `useMutation`, modal, or toast yet. Add a trash `<button>` revealed on row hover (`opacity-0 group-hover:opacity-100 focus:opacity-100`, `text-gray-400 hover:text-red-600`) whose `onClick` does `e.preventDefault(); e.stopPropagation()` so it neither navigates nor selects, opening a small Tailwind confirm dialog ("Delete this session? Biometric data and instructions will be permanently removed"). On confirm, run a React Query `useMutation` calling `apiFetch<void>(\`/sessions/runs/\${id}\`, { method: 'DELETE' })`; in `onSuccess` `invalidateQueries(['session-runs'])` and, if the deleted id equals the current `useParams().id`, `navigate('/sessions')`. Keep `SessionList` presentational by passing an `onDelete`/selected-id from `SessionsPage` (or a small `useDeleteSession` hook). Guard `apiFetch` for the 204 no-body response (`if (res.status === 204) return undefined as T` before `res.json()` in `src/core/api/client.ts`). Show the Delete button's pending spinner (LoginPage pattern) and surface 403/404/network errors inline in the dialog (`text-red-600`) rather than closing silently. Spec: `.ai-factory/notes/25-delete-session-from-list.md`. [12m 38s]

## Phase 17 — Fix: hide the delete affordance on a live session

The delete button (Phase 16) must never appear for a live/in-flight session — only finalized runs are deletable, and the backend rejects deleting a live one with 409 (mind_api note 56). The session list is already server-filtered to ended runs, but the dashboard reads in-flight data via endpoints that allow live reads, so the gate must be explicit. Depends on web Phase 16 + mind_api Phase 46. Context: `.ai-factory/notes/26-hide-delete-on-live-session.md`.

- [x] **Gate the trash button on `session.endedAt` + handle the 409 response** — In `SessionList.tsx` (and any live-session view that grows a delete control), render the delete `<button>` only when the session is finalized: `{session.endedAt != null && <button …/>}`. `SessionRun.endedAt` is typed non-null today (`src/core/types/index.ts`) because `listRuns` guarantees it — so the list gate is always true now; keep it as the documented single enforcement point and widen the type to `string | null` if a live-session row/detail view is added. In the Phase 16 delete mutation's error path, distinguish `ApiError.status === 409` ("Cannot delete a session that is still active") from 403/404/network: show the 409 message inline in the confirm dialog (`text-red-600`) and do NOT invalidate/remove the row — the session is still running. Verify: finalized row shows the button on hover; a session with null `endedAt` shows none; a 409 keeps the row with the inline message instead of closing silently. Spec: `.ai-factory/notes/26-hide-delete-on-live-session.md`. [6m 58s]

## Phase 18 — Fix: delete button overlays the row text instead of reserving a gutter

The trash button (Phase 16) is absolutely positioned, but the row content carries a permanent `pr-10` right padding that reserves a ~40px gutter on every row even when the button is hidden — the text is always squeezed. The button should float over the trailing text on hover with no permanently reserved zone. Layout-only follow-up to Phase 16. Context: `.ai-factory/notes/27-delete-button-overlay-not-gutter.md`.

- [x] **Drop the reserved gutter; overlay the hover button on the row text** — In `src/pages/SessionsPage/SessionList.tsx`, the content block `<div className="pointer-events-none px-4 py-3 pr-10">` reserves a fixed right gutter via `pr-10` for the absolutely-positioned trash button, so the title (`truncate`) and meta line are permanently narrowed even though the button only shows on hover. Remove the extra right reserve (use plain `px-4 py-3`) so the text spans the full width and truncates at the row edge; keep the button `absolute right-2 top-2 z-10` so on hover it floats **over** the trailing text. Add a backing behind the button so the trash glyph stays legible over the characters underneath — prefer a small rounded chip whose background matches the row in both states (unselected `group-hover:bg-gray-50 dark:bg-gray-900`, selected `bg-gray-100 dark:bg-gray-800`), or a left-edge `bg-gradient-to-l` fade. Guards: do NOT change the button's Phase 17 gate (`session.endedAt != null`), the `preventDefault`/`stopPropagation` click handling, the stretched-`Link` (`z-0`) / `pointer-events-none` text pattern, or the confirm-dialog wiring — layout/markup only. Verify legibility in light, dark, selected, and unselected rows. Spec: `.ai-factory/notes/27-delete-button-overlay-not-gutter.md`. [4m 13s]

## Phase 19 — Long-session chart performance (client render)

30 min+ meditation sessions freeze the chart: ~450k points/series rasterized per frame on a ~1500 px grid, plus a full `notMerge:true` rebuild and a whole-array re-sort on every chunk arrival. Fix = decimate what is *drawn* (not stored) and stop the per-chunk full rebuild/re-sort. Client-only — these remove the **freeze/lag (CPU/render)**, NOT the raw-memory footprint (that is Phase 20 / server LOD). Confirmed with the API owner: prefer `sampling:'minmax'` over `lttb` (spike preservation for EEG/HR), and never use naive `appendData` (out-of-order chunks → X zigzag). Order by risk: 28 → 31 → 29 → 30.

- [x] **Display decimation on biometric line series (`minmax`)** — 30 min+ sessions freeze because every raw point (~450k/series at 250 Hz) is rasterized each frame though the grid is ~1500 px wide. Add `sampling: 'minmax'` to the line config returned by `buildLineSeriesEntry` (`src/pages/SessionsPage/chartOption.ts:42-61`) so ECharts decimates the *drawn* points per the current zoom span/width and restores full detail on zoom-in from the intact `data`. `minmax` not `lttb` (API-owner flag): it keeps each bucket's min+max so EEG/HR spikes survive (echarts 6.1.0 supports it). Guards: line series only — do NOT touch the phase custom series (`chartOption.ts:302-360`); fixes the freeze, NOT memory (raw samples still held — Phase 20). Spec: `.ai-factory/notes/28-display-decimation-minmax.md`. [5m 0s]

- [x] **`large` + `progressive` render flags for dense line series** — every point is rasterized synchronously on each `setOption`. In `buildLineSeriesEntry` (`src/pages/SessionsPage/chartOption.ts:42-61`) add `large:true` + `largeThreshold` (~2000) for the optimized batch path, and `progressive` (~4000) + `progressiveThreshold` (~4000) so a big series paints across frames instead of blocking the main thread. Tune thresholds so short sessions stay below them (no behavior change). Line series use `symbol:'none'`, so large-mode's loss of per-point interactivity is irrelevant; the axis tooltip still works. Complementary to note 28 (sampling cuts zoomed-out points; large/progressive handle the deep-zoom full-res window). Guard: do NOT apply to the phase custom series. Spec: `.ai-factory/notes/31-large-progressive-render-flags.md`. [5m 8s]

- [x] **Sorted biometric accumulation — drop the per-rebuild re-sort** — `toSeries` (`src/pages/SessionsPage/transforms.ts:45-54`) sorts the projected pairs on every rebuild, and `buildSessionChartOption` re-runs per chunk (memo keyed on `biometrics`), so the whole accumulated array is re-sorted O(N log N) every 30 s of data. `useBiometricChunks` appends chunks `[...prev, ...data]` (`useBiometricChunks.ts:113`) unordered, and chunks arrive out of order under zoom. Keep `biometrics` globally time-sorted via merge-insert of each (sorted) incoming chunk, then drop the `.sort()` in `toSeries`. API-owner flag #2: this sorted guarantee is the prerequisite for any incremental update (note 30) — do NOT use naive `appendData` (tail-append, no sort → X zigzag on out-of-order chunks). Preserve the `fetchIdRef`/dedup refs. Spec: `.ai-factory/notes/29-sorted-biometric-accumulation.md`. [5m 41s]

- [ ] **Incremental chart update on stable grid structure** — the chart full-rebuilds (`notMerge:true`, `SessionCharts.tsx:164`) on every chunk — the biggest per-chunk cost. The rebuild exists only to avoid axis cross-wiring when a NEW sampleType/grid first appears in a later chunk (`chartOption.ts:63-76`). Have `buildSessionChartOption` also return a structure signature (ordered present-grid ids); track it in a ref in `SessionCharts` and pass `notMerge` only on first render or a signature change (full rebuild), else `notMerge:false` so ECharts merges by stable `id` and swaps each series' (sorted, note 29) `data` — NOT `appendData`. Guards: always full-rebuild on first render + any signature delta; keep `zoomRef` working both paths; `EChart` already keys setOption on `[option,notMerge,isDark]`. Highest-risk client task — ship after 28/31; defer if those already make rebuilds cheap. Spec: `.ai-factory/notes/30-incremental-chart-update-stable-structure.md`.

## Phase 20 — Server-side LOD: client consumer (gated on mind_api)

Even with Phase 19, the client still fetches and holds every raw sample of a long session — the raw memory/network footprint is only removed by loading a coarse resolution when zoomed out, served by the mind_api aggregated read (Phase 48 / api note 58). Contract is fixed: `?bucketSec=<n>` → per-bucket per-field min+max (mirrors note 28's `minmax`); omitted ⇒ today's raw path untouched. Response shape decided this side: **(i)** synthetic `BioSampleDto[]` (zero client churn — the existing pipeline renders it as-is). Gated on the endpoint shipping. Decomposes once it lands.

- [ ] **Consume server-aggregated biometrics for the zoomed-out overview** — even after Phase 19, `useBiometricChunks` still fetches and accumulates every raw sample of a long session; the raw memory/network footprint is only removed by loading a coarse resolution when zoomed out. Gated on the mind_api endpoint (Phase 48 / api note 58, contract fixed). This side owns: (1) response shape **(i)** — server emits 2 synthetic min/max `BioSampleDto` per bucket per sampleType, so `byType`→`toSeries(field)`→`buildLineSeriesEntry` render it unchanged (no new transform); requires distinct in-bucket timestamps min<max. (2) zoom→`bucketSec` policy — `RAW_SPAN_LIMIT` (~90 s, hysteresis) below which the raw chunked path is used, else `bucketSec = snapUp(spanSec/~1200, [1,2,5,10,15,30,60,120,300])`; fetch the overview in one request on mount. Switch on `datazoom`, reuse `zoomRef` + note 30's merge. `motion` is ~95 % of volume → Phase 19 decimation stays mandatory on the raw zoom-in path. Decompose into (a) coarse overview fetch+render and (b) zoom-driven resolution switch once the endpoint ships. Spec: `.ai-factory/notes/32-server-lod-client-consumer.md`.

---STOP---
